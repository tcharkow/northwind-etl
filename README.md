# Northwind ELT Pipeline

End-to-end ELT pipeline built with PostgreSQL, dbt, and FastAPI — powered by the classic Northwind dataset.

## Overview

ELT pipeline that loads raw Northwind CSV data into PostgreSQL, transforms it through dbt staging and mart models, and serves analytics results via a FastAPI backend consumed by a React + Plotly dashboard.

## Pipeline Architecture
